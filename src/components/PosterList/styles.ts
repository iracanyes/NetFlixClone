import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: 300
    },
    flatlist: {
        width: '100%',
        height: 200
    }
});

export default styles;